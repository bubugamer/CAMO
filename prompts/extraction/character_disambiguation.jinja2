你要判断下面两组人物证据是否指向同一角色。

规则：
1. 只能根据给定证据判断，不要引入书外知识。
2. 若只是名字相同，但身份、称谓、出现上下文明显不同，应判为不是同一角色。
3. 若名字不同，但别名、称谓、身份标签明显指向同一人，可判为同一角色。
4. 输出必须严格符合 JSON Schema，不要输出解释、不要输出 Markdown。

候选 A：
{{ left_cluster | tojson(indent=2) }}

候选 B：
{{ right_cluster | tojson(indent=2) }}

请输出：
- `same_character`: 是否同一角色
- `confidence`: 0 到 1
- `reason`: 简短说明判断依据
