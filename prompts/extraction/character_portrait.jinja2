你要为目标人物生成符合 CAMO PRD v0.2 / TDD v0.2 的“人物画像 + 关系 + 事件 + 记忆”结构化 JSON。

硬性要求：
1. 只能根据下面提供的证据片段总结，不要使用书外知识，不要补全未出现的信息。
2. 输出必须严格符合 JSON Schema，不要输出解释、不要输出 Markdown、不要输出代码块。
3. 顶层必须包含：
   - `schema_version`
   - `character_core`
   - `character_facet`
   - `relationships`
   - `events`
   - `memories`
4. `schema_version` 一律填写 `"0.2"`；`character_core.schema_version`、`character_facet.schema_version`、每个 `relationship.schema_version`、每个 `event.schema_version`、每个 `memory.schema_version` 也都必须是 `"0.2"`。
5. `source_segments`、`segment_ids` 只能使用下方证据中出现过的 `segment_id`。
6. `participant_names`、`related_character_names`、`target_name` 必须使用证据里实际出现的名字，不要臆造。
7. `memory_type` 只能是 `profile` 或 `episodic`。
8. `trait_profile` 必须是 0 到 100 的整数分值，不要写文字解释。
9. `behavior_profile`、`communication_profile`、`constraint_profile` 中的枚举值必须严格使用 Schema 允许的枚举。
10. `motivation_profile.primary`、`secondary`、`suppressed` 只能使用以下受控词表：
    - `power`
    - `status`
    - `wealth`
    - `survival`
    - `affiliation`
    - `altruism`
    - `curiosity`
    - `mastery`
    - `freedom`
    - `revenge`
    - `order`
    - `pleasure`
    - `truth`
    - `loyalty`
    - `fairness`
    - `honor`
    - `efficiency`
11. `forbidden_behaviors.namespace` 只能使用：
    - `meta`
    - `style`
    - `setting`
    - `plot`
    - `ethics`
    - `custom`
12. `forbidden_behaviors` 每项都必须是：
    - `namespace`
    - `tag`
    - `description`
13. `evidence_map` 的 key 必须指向 Character Core 的字段路径，例如：
    - `trait_profile.conscientiousness`
    - `motivation_profile.primary`
    - `behavior_profile.risk_preference`
    可用字段路径只允许来自以下集合：
    - `trait_profile.openness`
    - `trait_profile.conscientiousness`
    - `trait_profile.extraversion`
    - `trait_profile.agreeableness`
    - `trait_profile.neuroticism`
    - `motivation_profile.primary`
    - `motivation_profile.secondary`
    - `motivation_profile.suppressed`
    - `behavior_profile.conflict_style`
    - `behavior_profile.risk_preference`
    - `behavior_profile.decision_style`
    - `behavior_profile.dominance_style`
    - `communication_profile.tone`
    - `communication_profile.directness`
    - `communication_profile.emotional_expressiveness`
    - `communication_profile.verbosity`
    - `communication_profile.politeness`
    - `constraint_profile.knowledge_scope`
    - `constraint_profile.role_consistency`
    - `constraint_profile.forbidden_behaviors`
14. `temporal_snapshots` 必须按时间顺序排列，并为每个阶段提供：
    - `period_label`
    - `source_segments`
    - `display_hint`
    - `stage_summary`
    - `known_facts`
    - `unknown_facts`
    - `profile_overrides`
    - `notes`
15. `relationships` 必须只描述目标人物指向其他已知人物的关系；`timeline` 可为空数组，但若填写则每项都必须带：
    - `period_label`
    - `public_state`
    - `hidden_state`
    - `source_segments`
16. 如果证据不足：
    - 字符串字段可留空字符串
    - 数组字段返回空数组
    - 不要发明不存在的事件、口头禅、外貌或背景

目标人物：
- name: {{ character_name }}
- aliases: {{ aliases | tojson }}

已知项目角色清单（用于名字对齐，不代表一定都与目标人物有关）：
{{ known_character_list | tojson(indent=2) }}

证据片段：
{% for item in evidence_segments %}
### segment_id={{ item.segment_id }} position={{ item.position }}{% if item.chapter %} chapter={{ item.chapter }}{% endif %}
{{ item.excerpt }}

{% endfor %}

输出内容的语义要求：
- `character_core`：稳定人格、动机、行为模式、沟通风格、运行约束
- `character_facet`：证据地图、生平补充、阶段变化、抽取元信息
- `relationships`：目标人物对其他人物的关系边，包含公开态度、隐含态度和可选阶段变化
- `events`：从证据中可明确归纳的重要事件，优先 3-6 条；证据不足时可更少
- `memories`：
  - `profile`：稳定事实 / 长期画像
  - `episodic`：具体经历 / 场景记忆
